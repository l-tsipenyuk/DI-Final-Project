import { useEffect, useState } from "react";
import { useParams, Link, useNavigate } from "react-router-dom";

const Quiz = (props) => {

    useEffect(() => {
    }, []);

    return (
        <div>
            <h1>Here will be Quiz</h1>
            <Link to="/">Back to Homepage</Link>
        </div>

    )
}

export default Quiz;